%h1 Listing ratings

%table
  %tr
    %th User
    %th Num connections
    %th Num recommendations
    %th Num groups
    %th Num posts per month
    %th
    %th
    %th

  - @ratings.each do |rating|
    %tr
      %td= rating.user_id
      %td= rating.num_connections
      %td= rating.num_recommendations
      %td= rating.num_groups
      %td= rating.num_posts_per_month
      %td= link_to 'Show', rating
      %td= link_to 'Edit', edit_rating_path(rating)
      %td= link_to 'Destroy', rating, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Rating', new_rating_path
