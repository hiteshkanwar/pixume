= form_for @school_rating do |f|
  - if @school_rating.errors.any?
    #error_explanation
      %h2= "#{pluralize(@school_rating.errors.count, "error")} prohibited this school_rating from being saved:"
      %ul
        - @school_rating.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :score
    = f.number_field :score
  .actions
    = f.submit 'Save'
