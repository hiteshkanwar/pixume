= form_for @skill_set do |f|
  - if @skill_set.errors.any?
    #error_explanation
      %h2= "#{pluralize(@skill_set.errors.count, "error")} prohibited this skill set from being saved:"
      %ul
        - @skill_set.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
